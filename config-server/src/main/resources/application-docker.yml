spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Ahmettozcelik/microservice-config-repo.git

eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka

  instance:
    prefer-ip-address: true