server:
  port: 8080

spring:
  application:
    name: api-gateway
  profiles:
    active: local

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always